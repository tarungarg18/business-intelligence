# Verity — runtime dependencies
#
# Everything runs locally. No cloud provisioning is required for the prototype.

# --- Core ---
pandas>=2.0
numpy>=1.26
PyYAML>=6.0

# --- Structured store ---
duckdb>=1.0

# --- Knowledge graph ---
networkx>=3.0

# --- Analytical engine ---
statsmodels>=0.14        # STL decomposition, ETS forecasting
scikit-learn>=1.4        # Isolation Forest
shap>=0.44               # driver contribution

# --- Evidence Engine (retrieval) ---
# No torch, no ONNX runtime, no local model weights.
#
# Embeddings come from an API (Gemini primary, any OpenAI-compatible endpoint
# as fallback) over plain REST, with results cached to disk so the demo runs
# offline after the first build. With no key at all the system falls back to
# TF-IDF + SVD via scikit-learn, which is already a dependency.
#
# sentence-transformers was the original plan but pulls in ~2 GB of torch for
# a corpus of a few hundred chunks. Vector search at this scale is a numpy
# dot product; ChromaDB would be infrastructure without a purpose.
httpx>=0.27

# --- Service + UI ---
fastapi>=0.110
uvicorn>=0.29
streamlit>=1.33
pydantic>=2.0
